# Subtask Coordination Contract (auto-synthesized fallback)

The full structured planner could not produce a contract for this issue. This minimal contract is auto-derived from the basic task decomposition so agents at least agree on filenames.

## File ownership (BINDING)

Each subtask owns the files listed below. Other agents are filling in their own files in parallel; do not create files outside your list, do not rename, and do not invent new filenames. If your owned-file list is empty, prefer JOINING an existing file from another subtask rather than creating a new one.

- **w0** — Create the iOS-style HTML structure with all necessary UI elements for the Pomodoro timer (display, buttons, session indicator, etc.)
  - Files: `index.html`
- **w1** — Create the CSS styling with iOS design, SVG animations (e.g., circular progress ring), and responsive layout
  - Files: `styles/style.css`
- **w2** — Implement the main timer logic, state management (start, pause, reset, cycles), and update the UI in JavaScript
  - Files: `scripts/app.js`
- **w3** — Implement a sound manager using Web Audio API to play sounds for timer start, tick, and completion events
  - Files: `scripts/sound.js`
- **w4** — Generate background images using nano banana2 and save them as assets, integrate them via CSS
  - Files: `tools/generate_bg.py`, `assets/backgrounds/bg1.jpg`

## Hard rule
If the issue is a single self-contained artifact (e.g. one HTML file), all subtasks should EDIT THE SAME FILE rather than each creating a competing copy. Use the first subtask's filename as the canonical target.
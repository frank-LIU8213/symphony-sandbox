# Symphony Cooperation Plan for NEX-32

_Auto-generated by `plan_cooperation`. This file is committed before any sub-agent runs, so the PR diff captures the plan model committed to._

## Subtasks (3)

### w0: User can explore an interactive, animated 3D-style Pluto model

**Files owned (this agent may modify):**
- `js/pluto-model.js`
- `js/animations.js`
- `assets/pluto-model.svg`
- `css/animations.css`

**Files readonly (must not modify):**
- `js/types.js`
- `js/app.js`

**Acceptance criteria:**
- [ ] SVG model renders inside #pluto-model-container with distinct crust, mantle, and core layers
- [ ] Hovering or clicking a layer triggers a highlight animation and plays a subtle sfx
- [ ] Continuous rotation animation runs smoothly at 60fps
- [ ] setHighlight() and resetHighlight() APIs correctly toggle layer visibility and glow effects

### w1: User can scroll through educational content with synchronized audio cues

**Files owned (this agent may modify):**
- `js/sections.js`
- `js/content.js`
- `js/audio.js`

**Files readonly (must not modify):**
- `js/types.js`
- `js/app.js`
- `js/pluto-model.js`

**Acceptance criteria:**
- [ ] Content sections render with fade-in/slide-up transitions on scroll
- [ ] Scrolling into a section triggers the corresponding audioCue via audio.js
- [ ] Scrolling into a section calls pluto-model.setHighlight() to emphasize the relevant layer
- [ ] IntersectionObserver correctly handles fast scrolling without duplicate triggers

### w2: User gets a cohesive, responsive tech-feel interface with global controls

**Files owned (this agent may modify):**
- `index.html`
- `css/main.css`
- `js/app.js`
- `js/types.js`

**Files readonly (must not modify):**
- `js/pluto-model.js`
- `js/sections.js`
- `js/audio.js`

**Acceptance criteria:**
- [ ] Layout is fully responsive across mobile, tablet, and desktop viewports
- [ ] Tech-feel CSS variables and typography are applied consistently
- [ ] Master volume toggle button correctly mutes/unmutes all audio via setMasterVolume()
- [ ] All modules load without console errors and init() completes successfully

## Scaffold files (8)

- `js/types.js` (734 chars stub)
- `js/pluto-model.js` (464 chars stub)
- `js/sections.js` (375 chars stub)
- `js/app.js` (1787 chars stub)
- `index.html` (577 chars stub)
- `css/main.css` (1128 chars stub)
- `css/animations.css` (738 chars stub)
- `assets/pluto-model.svg` (712 chars stub)

## Shared Contract

See `.symphony-contract.md` (or the section below).

---

## Shared Types
- `AnimationState`: `{ id: string, duration: number, keyframes: string[] }`
- `AudioAsset`: `{ id: string, src: string, type: 'sfx' | 'ambient' }`
- `SectionData`: `{ id: string, title: string, content: string, svgId: string, audioCue: string, highlightLayer?: string }`
- `PlutoModelConfig`: `{ radius: number, rotationSpeed: number, layers: string[], metadata: Record<string, string> }`
- `PlutoModelAPI`: `render(container: HTMLElement, config: PlutoModelConfig): void`, `setHighlight(layer: string): void`, `resetHighlight(): void`
- `SectionAPI`: `renderSections(sections: SectionData[], container: HTMLElement): void`, `setupScrollObserver(): void`
- `AudioAPI`: `initAudio(): void`, `play(id: string, volume?: number): void`, `pauseAll(id?: string): void`, `setMasterVolume(v: number): void`, `registerAudioAssets(assets: AudioAsset[]): void`

## API Contract
- `animations.js`: `registerAnimation(config: AnimationState): void`, `trigger(id: string): void`, `cleanup(): void`
- `pluto-model.js`: `render(container: HTMLElement, config: PlutoModelConfig): void`, `setHighlight(layer: string): void`, `resetHighlight(): void`
- `sections.js`: `renderSections(sections: SectionData[], container: HTMLElement): void`, `setupScrollObserver(): void`
- `audio.js`: `initAudio(): void`, `play(id: string, volume?: number): void`, `pauseAll(id?: string): void`, `setMasterVolume(v: number): void`, `registerAudioAssets(assets: AudioAsset[]): void`
- `content.js`: `loadSections(): Promise<SectionData[]>`, `loadPlutoData(): PlutoModelConfig`
- `app.js`: `init(): Promise<void>`

## Naming Conventions
- Files: kebab-case for CSS/SVG, camelCase for JS.
- Exports: named exports for all public APIs.
- Types: PascalCase for interfaces/types.
- Data IDs: kebab-case (e.g., `sfx-hover`, `pluto-core`).
- CSS classes: BEM-like or utility-first, prefixed with `pluto-` (e.g., `pluto-section`, `pluto-model-layer`).
